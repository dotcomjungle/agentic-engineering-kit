# Client

The frontend client portion of the web app.

TODO: describe the role of the client — what it is responsible for (e.g., UI rendering, user interaction, client-side state, calling the server's API) and what it is explicitly *not* responsible for.
